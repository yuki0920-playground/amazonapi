<% if @results.present? %>
  <% @results.each do |result| %>
    <div class="list">
      <%= link_to (image_tag(result.image_url, style: "width: 70px;height: 100px;display: inline-block;")), result.url %>
      <%= result.title %>
    </div>
  <% end %>
<% end %>